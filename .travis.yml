language: c

os:
- linux

compiler:
- gcc

script:
- cd Source
- cd core
- mkdir build
- mkdir build/hal
- mkdir build/lib
- mkdir build/kernel
- ls -d */
- ls build/
- chmod -R 777 build
- make

addons:
    apt:
        packages:
          - nasm

